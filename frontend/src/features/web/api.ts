// import { apiClient } from "@/lib/api-client";
// import type { Organization } from "@/types/organization.model";

// export async function getOrganizations(): Promise<{role: string; organization: Organization}[]> {
//     const response = await apiClient(`/organizations`, { method: "GET" });
//     const data = await response.json()
//     const organizations = data.data.organizations as {role: string; organization: Organization}[];
//     return organizations;
// }
